[phases.setup]
# Node.js simples - OCR.space API não precisa de dependências nativas
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "npm start"

